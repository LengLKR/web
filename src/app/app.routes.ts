import { Routes } from '@angular/router';
import { LineProfileComponent } from './pages/line-profile/line-profile.component';

export const routes: Routes = [{ path: '', component: LineProfileComponent }];
